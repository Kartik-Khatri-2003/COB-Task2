<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password Page</title>
    <!-- External Font Import -->
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto+Slab&display=swap');

        /* Styles for the body element */
        body{
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(120deg, blue, pink);
            overflow: hidden;
            font-family: 'Roboto Slab', serif;
        }

        /* Styles for the main container */
        .box{
            color: aliceblue;
            width: 350px;
            height: auto;
            text-align: center;
            padding: 30px 35px;
            border-radius: 10px;
            background: transparent;
            border: 1px solid white;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(30px);
        }

        /* Styles for text input field */
        .text_field{
            font-size: 15px;
            border: none;
            outline: none;
            width: calc(100% - 65px);
            height: 40px;
            margin-top: 20px;
            margin-bottom: 10px;
            border-radius: 50px;
            padding: 5px 45px 5px 20px;
            text-align: center;
        }

        /* Styles for the submit button */
        button{
            text-align: center;
            border: none;
            outline: none;
            width: 100%;
            height: 50px;
            margin-bottom: 20px;
            border-radius: 50px;
            font-size: large;
        }

        /* Styles for the "Back to Login" link */
        .back{
            font-size: large;
            color: aliceblue;
            text-decoration: none;
            transition: all 0.3s;
            transform-origin: center;
        }

        /* Styles for the "Back to Login" link on hover */
        .back:hover{
            font-size: larger;
        }

        /* Styles for an informational message */
        .message {
            margin-left: 30px;
            margin-right: 30px;
        }
    </style>
</head>
<body>
    <!-- Main container for the "Forgot Password" form -->
    <div class="box">
        <h1>Forgot Password</h1>
        <p>Enter your email address, and we'll send you a link to reset your password.</p>
        <input class="text_field" type="text" placeholder="your email address..">
        <h4></h4>
        <button>Submit</button>
        <p><a href="login" class="back">Back to Login</a></p>
    </div>

    <!-- JavaScript for handling form submission and displaying messages -->
    <script>
        let btn = document.querySelector("button");
        let inp = document.querySelector("input");
        let message = document.querySelector("h4");

        // Add an event listener to the button
        btn.addEventListener("click", function(){

            // Add a class to the message element to make it visible
            message.classList.add("message");
            
            // Check if the input value is empty
            if(inp.value == ""){
                message.innerHTML="Please fill in your email";
            }
            else{
                // Clear the input field and display a success message
                inp.value = "";
                message.innerHTML="A link has been sent to your email to reset your password";
            }     
        })
    </script>
</body>
</html>
